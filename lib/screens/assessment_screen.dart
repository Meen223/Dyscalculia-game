import 'package:flutter/material.dart';

class AssessmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Assessment Screen'),
      ),
      body: Center(
        child: Text('This is the Assessment Screen.'),
      ),
    );
  }
}
